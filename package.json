{
  "name": "brand-mention-tracker",
  "version": "1.0.0",
  "description": "MERN stack brand mention tracker with AI sentiment analysis",
  "main": "backend/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "start": "cd backend && npm start",
    "build": "cd frontend && npm run build",
    "install-deps": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["mern", "brand-monitoring", "ai", "sentiment-analysis"],
  "author": "Shashi",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}